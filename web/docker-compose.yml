version: '2.3'

services:

  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "80:80"
      - "443:443"
    environment:
      - NGINX_HOST=muz.ca
      - NGINX_PORT=80
    volumes:
      - letsencrypt:/letsencrypt

volumes:
  letsencrypt: